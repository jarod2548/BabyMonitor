spring:
  datasource:
    url: jdbc:postgresql://db:5432/babymonitor
    username: BabyMonitor
    password: N8v$3xQp!7rLzB2w
  flyway:
    baseline-on-migrate: true
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    org:
      flywaydb=DEBUG: